/**
 * Provides classes for every state the game can be in.
 * <p>
 * These states include <code>MenuState</code>,
 * <code>GameState</code>, <code>AboutState</code> and so on.
 * Every state has its own distinguishable render method,
 * which ensures the user sees only the state the game is
 * currently in.
 */
package cz.cuni.mff.java.klima.pacman.states;